# Remap right win key to be left win key
xmodmap -e 'keycode 135 = Super_R' && xset -r 135
# Remap web next key to be win key:
# xmodmap -e 'keycode 167 = Super_R' && xset -r 167
# Remap web prev key to be win key:
# xmodmap -e 'keycode 166 = Super_R' && xset -r 166
